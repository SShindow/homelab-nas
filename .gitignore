# OS files
.DS_Store
Thumbs.db

# Editor/IDE
.vscode/
.idea/

# Secrets & credentials — never commit these
*.key
*.pem
*credentials*
*.env
.env.*

# Local scratch/config exports that may contain sensitive paths or IPs
configs/*.local.*

# Tailscale / API keys — never commit these
*tskey*
*authkey*
*.token
